<body>
  <h1>students</h1>
  <ul>
    <% students.forEach(student => { %>
      <li>
				<%= student.name %> 
				- 
				<%= student.Jedi ? 'Jedi' : 'not Jedi' %>
			</li>
    <% }) %>
  </ul>
</body>